#ifndef UTILS_H_
#define UTILS_H_

#include <vector>
#include <string>

const unsigned short int ALPHABET_SIZE = 255;

std::vector<int> Borders(std::string pat);
std::string to_string(std::string path);

#endif
